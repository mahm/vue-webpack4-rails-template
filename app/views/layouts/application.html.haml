!!! 5
%html
  %head
    %meta{ charset: 'utf-8' }
    %meta{ name: 'viewport', content: 'width=device-width, initial-scale=1' }
    %title
      = content_for?(:title) ? yield(:title) : 'vue-webpack4-rails-template' 
    = csrf_meta_tags
    = csp_meta_tag
    = stylesheet_pack_tag 'application', media: 'all'
    = javascript_include_polyfill_tag
    = javascript_pack_tag 'application'
  %body
    #vue-app
      .container
        = yield
